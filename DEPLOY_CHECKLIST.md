# ✅ Чеклист готовности к деплою

## 📋 Проверка перед деплоем

### ✅ Код
- [x] Удалены все debug логи из клиентского кода
- [x] Нет ошибок линтера
- [x] Все импорты корректны
- [x] TypeScript компилируется без ошибок

### ✅ Конфигурация
- [x] `package.json` - все зависимости указаны
- [x] `next.config.js` - настроен для Vercel
- [x] `tsconfig.json` - корректная конфигурация
- [x] `vercel.json` - правильные настройки деплоя
- [x] `.gitignore` - исключены ненужные файлы

### ✅ Файлы и ресурсы
- [x] Все изображения в `public/assets/`
- [x] Пути к изображениям корректны (`/assets/...`)
- [x] Логотип доступен по `/assets/logo_mt.png`
- [x] Фон доступен по `/assets/image.png`

### ✅ Метаданные
- [x] Домен `mytanks.online` указан в `layout.tsx`
- [x] Open Graph метаданные настроены
- [x] Twitter Card метаданные настроены
- [x] Canonical URL указан

### ✅ Страницы
- [x] Главная страница (`/`)
- [x] Магазин (`/donate`) - редирект на Discord
- [x] Правила игры (`/rules`)
- [x] Политика конфиденциальности (`/privacy`)
- [x] Пользовательское соглашение (`/terms`)

### ✅ Компоненты
- [x] Header с навигацией и мобильным меню
- [x] SnowEffect работает
- [x] Свайпы для мобильного меню работают

### ✅ API
- [x] `/api/discord-news` - готов к работе (требует переменные окружения)

## 🚀 Инструкция по деплою

### 1. GitHub
```bash
git init
git add .
git commit -m "Initial commit: MyTanks website"
git branch -M main
git remote add origin https://github.com/yourusername/mytanks-website.git
git push -u origin main
```

### 2. Vercel
1. Перейти на vercel.com
2. Войти через GitHub
3. New Project → выбрать репозиторий
4. Deploy

### 3. Домен
1. Settings → Domains
2. Добавить `mytanks.online`
3. Настроить DNS записи

### 4. Переменные окружения (опционально)
- `NEXT_PUBLIC_GAME_URL` - URL для скачивания игры
- `DISCORD_BOT_TOKEN` - для API новостей
- `DISCORD_CHANNEL_ID` - для API новостей

## ✨ Готово к деплою!
